package uk.ac.manchester.skeleton;

/**
 * A convenient place to declare XML namespaces. These need to match in a few
 * key places (for sanity if nothing else) so it is easiest to factor them out
 * into a common location. Note that we're just declaring constants here, not
 * methods.
 */
interface Namespaces {
	/** The application's XML namespace. */
	String NAMESPACE = "http://cs.manchester.ac.uk/skeleton/webapp";
}
